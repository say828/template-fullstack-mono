# App Screen Verification

- current status: `partial`
- retained references: `sdd/02_plan/03_architecture/spec_traceability.md`, `sdd/03_verify/10_test/ui_parity/web_agentic_dev_latest.json`
- direct verification:
  - `pnpm --dir client/web build`
  - `python3 sdd/99_toolchain/01_automation/validate_template_screen_ir.py --service seller`
  - `cd server && .venv/bin/python -m pytest tests/test_password_reset_request.py`
  - `cd server && .venv/bin/python -m pytest tests/test_identity_bootstrap.py`
- `python3 sdd/99_toolchain/01_automation/run_playwright_exactness.py --suite frt-005-signup-type --base-url http://127.0.0.1:3003`
- `python3 sdd/99_toolchain/01_automation/run_playwright_exactness.py --suite frt-014-active-detail --base-url http://127.0.0.1:5173`
- `python3 sdd/99_toolchain/01_automation/run_playwright_exactness.py --suite frt-016-bids --base-url http://127.0.0.1:5173`
- `python3 sdd/99_toolchain/01_automation/run_playwright_exactness.py --suite frt-018-winner-select --base-url http://127.0.0.1:5173 --api-base-url http://127.0.0.1:8000/api/v1`
- `python3 sdd/99_toolchain/01_automation/run_browser_use_diagnostic.py --suite frt-005-signup-type --base-url http://127.0.0.1:3003`
  - `curl -I http://127.0.0.1:5173/seller/vehicles/123/inspection`
  - `curl -I http://127.0.0.1:5173/seller/vehicles/123/depreciation`
  - `curl -I http://127.0.0.1:5173/seller/vehicles/123/delivery-settlement-progress`
  - `curl -I http://127.0.0.1:5173/seller/vehicles/register`
  - `curl -I http://127.0.0.1:5173/seller/vehicles/register/confirm`
  - `curl -I http://127.0.0.1:5173/seller/settlement`
  - Playwright route smoke for `FRT_019 ~ FRT_037` on `http://127.0.0.1:5173`
  - Playwright route smoke for `FRT_013 ~ FRT_018` on `http://127.0.0.1:5173` using a locally created seller vehicle with bids
- route variant verification: `http://localhost:3003/login?role=dealer`에서 dealer 승인 안내 노출 및 social login 미노출 확인
- direct route check: `http://localhost:3003/forgot-password?role=seller`, `http://localhost:3003/forgot-password?role=dealer`, `http://localhost:3003/signup`
- `FRT_010` route check: `http://127.0.0.1:5173/seller/vehicles/initial` direct render + artifact `sdd/03_verify/02_screen/app/artifacts/frt_010_initial_runtime.png`; seller vehicle list filter box now contains the `FRT_013` 7-chip set (`전체 / 입찰 중 / 입찰 마감 / 검차 / 감가 협의 / 인도/정산 / 거래 완료`) with black active pill styling, and `최신 등록순` plus search input stay on the same row inside the card.
- `FRT_011` route check: `client/web/src/components/Layout.tsx` header 알림 버튼은 route push 없이 현재 페이지 modal overlay만 열고, modal anchor는 header 우측 상단 기준 `md:right-56 / top-[44px] / max-w-[304px]`로 조정했으며, modal open 시 본문은 그대로 보이되 반투명 full-screen backdrop가 덮도록 했다. legacy notifications page surface는 제거됐다.
- `FRT_012` route check: `client/web/src/components/Layout.tsx` header 프로필 버튼은 route push 없이 현재 페이지 modal overlay만 열고, panel은 `top-[-24px]`와 `py-[6px]`로 compact하게 유지한다. legacy profile page surface는 제거됐다.
- `FRT_013 ~ FRT_018` route smoke: `/seller/vehicles`, `/seller/vehicles/:vehicleId`, `/seller/vehicles/:vehicleId/images`, `/seller/vehicles/:vehicleId/bids`, `/seller/vehicles/:vehicleId/winner`, `/seller/vehicles/:vehicleId/detail/closed`
- `FRT_013 ~ FRT_018` runtime notes: `SellerVehicleDetailPage` now uses the shared `vehicle-hero` crop asset and surfaces a closed-state banner; `SellerVehicleWinnerSelectPage` now renders the page 19 selection layout, allows selecting a candidate bid, and confirms that bid through the close API.
- `FRT_013` route note: `/seller/vehicles` header CTA `새 차량 등록하기` is right-aligned in the list toolbar, matching the seller top-right chrome expectation.
- `FRT_013` card action note: `감가 내용 확인`, `검차 일정 확인`, `정산 확인` cards now show a left `상세 보기` button alongside the primary action, completed cards keep the single full-width detail action, the out-of-spec `마감 처리 / 강제 마감` buttons were removed from the list card footer, and the settlement card primary action routes to `/seller/settlement/pending/:vehicleId`.
- `FRT_013` status tag note: image-top state badges now use `감가 협의=gold`, `검차=green`, `입찰 마감=pink-violet`, `인도/정산=brown-orange`, `거래 완료=black-gray`.
- `FRT_014` route note: `/seller/vehicles/:vehicleId` active detail keeps `header -> hero/media + bidding summary -> vehicle identity -> vehicle basics -> photo strip -> trade progress -> recent activity` order, and the `vehicle identity` block now sits as a full-width row under the first hero/summary grid so it matches `sdd/01_planning/02_screen/ir/seller/FRT_014/ui_grid.csv` more closely.
- `FRT_014` exactness run note: `frt-014-active-detail` e2e suite는 ACTIVE 차량 상세에서 차량 기본정보/옵션/상태 태그 바인딩, 타이머 노출, 이미지 뷰어, `입찰 현황 보기` CTA를 검증한다.
- `FRT_014` data note: the photo/options card now renders persisted `vehicle.options` labels and the seller registration confirm payload forwards the same labels into the create API.
- `FRT_014` data note: the vehicle basics and registration confirm summary now render persisted `transmission` values instead of a hardcoded default label.
- `FRT_014` data note: the photo tiles open a modal image viewer, and the viewer moves left/right through persisted S3-backed `photo_urls` in stored order. visible captions were removed and the modal now matches the `FRT_015` fullscreen overlay large-image layout.
- `FRT_014` runtime note: the `차량 정보 수정` button is now active and opens `/seller/vehicles/:vehicleId/edit`, which saves through the seller update API.
- `FRT_014` validation note: `pnpm --dir client/web build` and `python3 sdd/99_toolchain/01_automation/validate_template_screen_ir.py --service seller` pass after the active detail shell moved `vehicle identity` out of the left hero column into its own full-width row.
- `FRT_017 / FRT_019 / FRT_022 / FRT_024 / FRT_027` route notes: the downstream detail-state companion docs now spell out the state-specific hero, summary card, and stepper anchors so the later runtime passes can reuse the same shell vocabulary.
- `FRT_016` route note: `/seller/vehicles/:vehicleId/bids`는 `입찰 목록 테이블`, `최상단 고정 차량 상세 보기`, `카운트다운` 동작, 토글 저장 반영, `더 보기 +` 스크롤 보존까지 정렬을 완료했다.
- `FRT_018` route note: `/seller/vehicles/:vehicleId/winner` now follows the screen spec page 19 structure with top vehicle summary, bid list, selection summary panel, and selected-bid close confirmation.
- `FRT_018` exactness note: `frt-018-winner-select` Playwright suite passes on `http://127.0.0.1:5173` against the local API and verifies selection state, summary panel updates, and confirm-button enablement.
- current validation note: `pnpm --dir client/web build` and `python3 sdd/99_toolchain/01_automation/validate_template_screen_ir.py --service seller` pass after the `FRT_014` active-shell alignment and the downstream detail-state UI spec expansion.
- current validation note: `pnpm --dir client/web build` passes after the shared `surface` 12-column token and shell wrapper migration.
- current verification note: `cd server && .venv/bin/python -m pytest tests/test_vehicle_options_integration.py` passes and covers vehicle option, transmission, and photo metadata create/detail serialization plus local demo seed persistence.
- local validation dataset: seller `sl@template.com` plus dealer `dl1@template.com`/`dl2@template.com`, operational vehicle set `11가 1301`, `11가 1302`, `11가 1303`, `11가 1304`, `11가 1307`, `11가 1308`, and representative bid/workflow states covering ACTIVE, CANCELLED, INSPECTION, DEPRECIATION, SETTLEMENT, COMPLETED
- local validation dataset: vehicle option, transmission, and photo metadata seeds now exist on the operational rows, including `11가 1303` with `블루투스 / 스마트크루즈 / 후방카메라 / 열선시트(앞좌석)`, `transmission=AUTO`, `photo_names=전면 / 후면 / 실내 / 계기판 / 타이어 / 측면`, and S3-backed `photo_urls` for the same six images.
- runtime check: `GET http://127.0.0.1:8000/api/v1/vehicles/my` with `sl@template.com` returns the 6 operational vehicles in `입찰 현황 -> 감가 내용 확인 -> 검차 -> 입찰자 선택하기 -> 정산 확인 -> 상세 보기` order, and direct DB query shows `11가 1307` has `workflow_count = 1` with `current_stage = SETTLEMENT` so its list badge resolves to `인도/정산`, while the other representative sold rows still carry their inspection/depreciation/completed workflows
- runtime check: `SellerVehiclesPage` now uses `seller/vehicles/:vehicleId` as the left-side detail route for `감가 내용 확인` and `검차 일정 확인`, while the `정산 확인` card primary action opens the seller settlement pending detail route instead of collapsing those states into a single-button row.
- route runtime fix check: `http://localhost:3003/signup/seller`에서 `판매자 회원가입` heading 렌더 확인
- `FRT_038 ~ FRT_049` route smoke: `/settings/account`, `/settings/settlement`, `/settings/settlement/empty`, `/settings/settlement/edit`, `/settings/notifications`, `/settings/security`, `/settings/locale`, `/settings/terms`, `/support/faqs`, `/support/notices`, `/seller/support/notices/:noticeId`, `/support/inquiries`
- `FRT_038 ~ FRT_049` screenshots: `sdd/03_verify/02_screen/app/artifacts/worker3-*.png`
- API check: `POST http://localhost:3003/api/v1/auth/password-reset/request`
- current verification note: `pnpm --dir client/web exec tsc -p tsconfig.json --noEmit` and `pnpm --dir client/web build` both pass after the `FRT_011` modal-only trigger change.
- exactness result: `FRT_005` Playwright suite pass, Browser Use diagnostic pass, artifacts at `sdd/99_toolchain/02_exactness/results/frt-005-signup-type-*`
- regression scope: `/login?role=seller`, `/login?role=dealer`, `/forgot-password?role=seller`, `/forgot-password?role=dealer`, `/signup`, `/seller/vehicles/initial`, `/seller/vehicles/:vehicleId`, `/seller/vehicles/:vehicleId/edit`, `/seller/vehicles/register/confirm`, `/seller/vehicles/:vehicleId/images`, `/settings/account`, `/settings/settlement`, `/settings/settlement/edit`, `/settings/settlement/empty`, `/settings/notifications`, `/settings/security`, `/settings/locale`, `/settings/terms`, `/support/faqs`, `/support/notices`, `/seller/support/notices/:noticeId`, `/support/inquiries`, shared public auth shell, seller header chrome, seller screen IR registry ordering, seller notifications/profile modal-only trigger, `vehicle.options` persistence/serialization, `vehicle.transmission` persistence/serialization, `vehicle.photo_names` persistence/serialization, `vehicle.photo_urls` rendering, local demo bootstrap option seeding
- residual risk: current retained build is green, but `FRT_013 ~ FRT_037` 상태 variation과 `FRT_038 ~ FRT_045` settings dense form details는 다음 visual pass에서 spacing/typography를 더 조일 여지가 있다. Browser Use is diagnostic support이며 final gate는 Playwright exactness와 route/function check를 따른다. `FRT_010 ~ FRT_012`는 shared header modal state라 추후 shell 변경 시 회귀 확인이 다시 필요하다.
