# Feature Index

이 디렉터리는 canonical feature spec 모음이다.

## Rule

- feature spec 파일명은 기본적으로 `<domain>_feature_spec.md`를 사용한다.
- upstream 입력이 spreadsheet, PRD, ticket이어도 구현 전에는 canonical markdown feature spec으로 정리한다.
- actor, use case, domain rule, side effect, completion condition을 최소 기준으로 남긴다.
